[pytest]
# Each plugin folder is an independent package with its own test modules
# (several of them named test_plugin.py / conftest.py). Import by full path so
# running the whole workspace at once doesn't hit basename collisions.
addopts = --import-mode=importlib
